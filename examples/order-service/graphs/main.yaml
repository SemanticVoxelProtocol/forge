name: order-service
description: 电商订单服务入口

input:
  - name: request
    type: OrderRequest

output:
  - name: result
    type: OrderResult

nodes:
  - id: pipeline
    type: order-pipeline

wires:
  - from: input.request      → to: pipeline.request
  - from: pipeline.result    → to: output.result
